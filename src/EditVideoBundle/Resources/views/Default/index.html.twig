{{ form_start(get_videos_profile_form_ajax, { 'attr' : {'novalidate': 'novalidate', 'role' : 'form', 'id': 'get-videos-profile-ajax'|default('form')} }) }}
{{ form_end(get_videos_profile_form_ajax) }}

Hello World!

<script>
    
    function EditVideoBundle()
    {
        getVideosProfile();
    }
    
    // pasar a su respectivo bundle; este bundle se activa (refresca) solamente cuando el usuario inicia la sesion... o en un intervalo de 1 hora
    function getVideosProfile()
    {
{#        alert("getVideosProfile");#}
        
        var form = $('#get-videos-profile-ajax');
        var url = form.attr('action');

        $.post(url, function (data)
        {
            {#alert("GET VIDEOS PROFILE works terrific");#}
        }).fail(function () {
            alert('GET VIDEOS PROFILE ERROR');
        });
        
    }
</script>